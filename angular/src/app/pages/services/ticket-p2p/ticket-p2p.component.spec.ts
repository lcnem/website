import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TicketP2pComponent } from './ticket-p2p.component';

describe('TicketP2pComponent', () => {
  // let component: TicketP2pComponent;
  // let fixture: ComponentFixture<TicketP2pComponent>;

  // beforeEach(async(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [ TicketP2pComponent ]
  //   })
  //   .compileComponents();
  // }));

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(TicketP2pComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });
});
