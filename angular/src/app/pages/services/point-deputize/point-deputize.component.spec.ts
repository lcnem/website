import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PointDeputizeComponent } from './point-deputize.component';

describe('PointDeputizeComponent', () => {
  // let component: PointDeputizeComponent;
  // let fixture: ComponentFixture<PointDeputizeComponent>;

  // beforeEach(async(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [ PointDeputizeComponent ]
  //   })
  //   .compileComponents();
  // }));

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(PointDeputizeComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });
});
