import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ContractDevelopmentComponent } from './contract-development.component';
/*
describe('ContractDevelopmentComponent', () => {
  let component: ContractDevelopmentComponent;
  let fixture: ComponentFixture<ContractDevelopmentComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ContractDevelopmentComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ContractDevelopmentComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/