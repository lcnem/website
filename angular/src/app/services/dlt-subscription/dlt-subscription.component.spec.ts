import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DltSubscriptionComponent } from './dlt-subscription.component';
/*
describe('DltSubscriptionComponent', () => {
  let component: DltSubscriptionComponent;
  let fixture: ComponentFixture<DltSubscriptionComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DltSubscriptionComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DltSubscriptionComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/